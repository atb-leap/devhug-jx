buildPack: leap-react-ts
pipelineConfig:
  pipelines:
    pullRequest:
      postBuild:
        steps:
        - name: deploy-to-cloud-run
          command: gcloud
          args:
            - --quiet
            - beta
            - run
            - deploy
            - --help
